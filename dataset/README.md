# Dataset Placeholder

This directory is intentionally kept empty in the public repo.

Place generated or curated `.jsonl` training data here when running local experiments. The package-level `.gitignore` keeps dataset contents out of version control while preserving this note.
